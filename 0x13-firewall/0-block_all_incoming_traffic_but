#Updating the system
sudo apt-get update

#Setupping the ufw seetings
sudo apt-get install -y ufw
sudo ufw default deny incoming
sudo ufw default allow outgoing

#Allowign PORTS : 22, 80, 443
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 443

#enabling ufw
sudo ufw enable
